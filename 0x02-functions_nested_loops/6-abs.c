#include <stdio.h>
#include "main.h"
/**
  * _abs - Computes absolute value of an integer
  * @num: A random integer
  * Return: 0 Always success
  */
int _abs(int num)
{
	if (num > 0)
		return (num);
	else
		return (-num);
}
